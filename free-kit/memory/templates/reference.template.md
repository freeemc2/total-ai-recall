---
name: reference-[short-name]
description: [one line — what this points to]
temperature: cool
type: reference
---

# [What This Is]

[A pointer to something the AI should know how to find: a link, a resource, a service you use, where something lives. Keep it to what's needed.]

**IMPORTANT — do not put real secrets here.** No passwords, API keys, or account numbers in memory files. If a memory needs to reference an account, name the account and where the key is stored (e.g. "the email API — key is in the project's .env file, not here"). Memory files can end up in git; secrets never should.
